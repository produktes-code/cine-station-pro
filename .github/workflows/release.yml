name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

permissions: contents: write
jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          cd backend
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
          pyinstaller --name cinestation_backend --onefile --hidden-import uvicorn --hidden-import fastapi --hidden-import celery.fixups --hidden-import celery.fixups.django app/main.py
      - run: |
          mkdir -p backend/dist
          if [ "$RUNNER_OS" == "Windows" ]; then
            chmod +x backend/dist/cinestation_backend.exe || true
          else
            chmod +x backend/dist/cinestation_backend || true
          fi
        shell: bash
      - run: npm ci || npm install
        working-directory: ./.
      - run: npm run build --if-present
        working-directory: ./.
      - run: npx electron-builder --${{ matrix.os == 'macos-latest' && 'mac' || 'win' }} --publish never
        working-directory: ./.
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-build
          path: |
            */dist-electron/*.dmg
            */dist-electron/*.exe
            dist-electron/*.dmg
            dist-electron/*.exe
            */dist/*.dmg
            */dist/*.exe
            dist/*.dmg
            dist/*.exe

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/**/*.dmg
            dist/**/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
